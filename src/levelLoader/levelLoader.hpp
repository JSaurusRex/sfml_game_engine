#ifndef LEVELLOADER_HPP
#define LEVELLOADER_HPP

#include <string>

class LevelLoader
{

    public:

    // LeveLoader(std::string file) : file(file)
    // { }

    static bool Load(std::string file);
};

#endif